# `raw` - Responsabilidade da Pasta

Conter os dados originais brutos, sem qualquer alteração.

## Regras

- Não sobrescrever nem editar manualmente os arquivos.
- Manter nomes e estrutura que permitam rastrear a origem.
- Usar estes dados como base para gerar `interim/` e `processed/`.
